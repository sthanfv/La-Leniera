import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // ✅ MANDATO-FILTRO: Dependencias y versiones actualizadas + Strict Mode
  // Removed ignoreBuildErrors and ignoreDuringBuilds to enforce quality.

  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'placehold.co',
        port: '',
        pathname: '/**',
      },
      {
        protocol: 'https',
        hostname: 'images.unsplash.com',
        port: '',
        pathname: '/**',
      },
      {
        protocol: 'https',
        hostname: 'picsum.photos',
        port: '',
        pathname: '/**',
      },
    ],
  },
  experimental: {
    // serverActions: true, // v14+ default
  },
  // Security Headers are also handled in middleware, but adding basic ones here is good practice.
  async headers() {
    return [
      {
        source: '/(.*)',
        headers: [
          { key: 'X-Content-Type-Options', value: 'nosniff' },
          { key: 'X-Frame-Options', value: 'DENY' },
          { key: 'Referrer-Policy', value: 'strict-origin-when-cross-origin' },
        ],
      },
    ];
  },
};

export default nextConfig;
